# -_aplicacion_java_intelliJ_idea_oracle-19C_- :.
Aplicacion Java + IntelliJ IDEA + Oracle 19c:

<img width="1536" height="1024" alt="image" src="https://github.com/user-attachments/assets/b13263d9-0fb4-41e7-b0ce-de94e765208b" />  

```
Condicionales Anidados para Estado de Producto
Descripcion

Este proyecto en Java permite:
Registrar productos en Oracle Database 19c
Utilizar JDBC
Aplicar tres condicionales anidados
Determinar automáticamente el estado del producto
Insertar información en la base de datos
Consultar registros almacenados

Tecnologías Utilizadas
Java 21
IntelliJ IDEA
Oracle Database 19c
JDBC
SQL
Lógica de Condicionales Anidados

El sistema evaluará:
Primer Condicional
if (cantidad > 0)

Segundo Condicional
if (precio >= 100000)

Tercer Condicional
if (categoria.equalsIgnoreCase("TECNOLOGIA"))

Estados del Producto
Condición	                          |  Estado
Stock > 0 + Precio alto + Tecnología  |	 PRODUCTO PREMIUM
Stock > 0 + Precio bajo	              |  PRODUCTO ECONOMICO
Sin stock	                          |  AGOTADO

SCRIPT ORACLE 19c
CREATE TABLE PRODUCTOS (
    ID NUMBER GENERATED BY DEFAULT AS IDENTITY,
    NOMBRE VARCHAR2(100),
    CATEGORIA VARCHAR2(100),
    PRECIO NUMBER(10,2),
    CANTIDAD NUMBER,
    ESTADO VARCHAR2(100)
);

Proyecto Java:
Main.java
import java.sql.Connection;
import java.sql.DriverManager;
import java.sql.PreparedStatement;
import java.util.Scanner;

public class Main {

    static final String URL =
            "jdbc:oracle:thin:@localhost:1521:XE";

    static final String USER = "system";
    static final String PASSWORD = "12345";

    public static void main(String[] args) {

        Scanner sc = new Scanner(System.in);

        try {

            Class.forName("oracle.jdbc.driver.OracleDriver");

            Connection conexion =
                    DriverManager.getConnection(
                            URL,
                            USER,
                            PASSWORD
                    );

            System.out.println("=================================");
            System.out.println(" REGISTRO DE PRODUCTOS");
            System.out.println(" ORACLE DATABASE 19c");
            System.out.println("=================================");

            System.out.print("Nombre del producto: ");
            String nombre = sc.nextLine();

            System.out.print("Categoria: ");
            String categoria = sc.nextLine();

            System.out.print("Precio: ");
            double precio = sc.nextDouble();

            System.out.print("Cantidad: ");
            int cantidad = sc.nextInt();

            String estado;

            // CONDICIONALES ANIDADOS

            if (cantidad > 0) {

                if (precio >= 100000) {

                    if (categoria.equalsIgnoreCase("TECNOLOGIA")) {

                        estado = "PRODUCTO PREMIUM";

                    } else {

                        estado = "PRODUCTO COSTOSO";

                    }

                } else {

                    estado = "PRODUCTO ECONOMICO";

                }

            } else {

                estado = "AGOTADO";

            }

            System.out.println("---------------------------------");
            System.out.println("ESTADO DEL PRODUCTO: " + estado);

            String sql =
                    "INSERT INTO PRODUCTOS " +
                    "(NOMBRE, CATEGORIA, PRECIO, CANTIDAD, ESTADO) " +
                    "VALUES (?, ?, ?, ?, ?)";

            PreparedStatement ps =
                    conexion.prepareStatement(sql);

            ps.setString(1, nombre);
            ps.setString(2, categoria);
            ps.setDouble(3, precio);
            ps.setInt(4, cantidad);
            ps.setString(5, estado);

            int resultado = ps.executeUpdate();

            if (resultado > 0) {

                System.out.println("---------------------------------");
                System.out.println("PRODUCTO REGISTRADO CORRECTAMENTE");

            } else {

                System.out.println("ERROR AL REGISTRAR");

            }

            ps.close();
            conexion.close();

        } catch (Exception e) {

            System.out.println("---------------------------------");
            System.out.println("ERROR");
            System.out.println(e.getMessage());

        }

    }

}

Dependencia JDBC Oracle
Descargar
JDBC Driver Oracle (ojdbc11.jar)
Agregar Librería en IntelliJ IDEA
File
→ Project Structure
→ Libraries
→ Add
→ Seleccionar ojdbc11.jar

Ejemplo de Ejecución
=================================
 REGISTRO DE PRODUCTOS
 ORACLE DATABASE 19c
=================================

Nombre del producto: Laptop Gamer
Categoria: TECNOLOGIA
Precio: 4500000
Cantidad: 5

---------------------------------
ESTADO DEL PRODUCTO: PRODUCTO PREMIUM

---------------------------------
PRODUCTO REGISTRADO CORRECTAMENTE

Consulta SQL:
SELECT * FROM PRODUCTOS;

Resultado Esperado en Oracle
ID |	NOMBRE	     | CATEGORIA  |	 PRECIO	  |  CANTIDAD	|  ESTADO
1	 |  Laptop Gamer | TECNOLOGIA |	 4500000	|  5	      |  PRODUCTO PREMIUM

Estructura del Proyecto IntelliJ
ProyectoProductos/
│
├── src/
│   └── Main.java
│
├── lib/
│   └── ojdbc11.jar
│
└── ProyectoProductos.iml

Funcionalidades del Sistema:
Registro de productos
Evaluación automática del estado
Uso de condicionales anidados
Inserción de datos en Oracle 19c
Manejo básico de errores
Conexión JDBC con Oracle

Salidas Posibles del Sistema:
Caso	                                    |  Resultado
Producto tecnológico costoso y con stock	|  PRODUCTO PREMIUM
Producto costoso sin ser tecnología	      |  PRODUCTO COSTOSO
Producto económico	                      |  PRODUCTO ECONOMICO
Producto sin existencia	                  |  AGOTADO

Requisitos:
Software Necesario
JDK 21
IntelliJ IDEA
Oracle Database 19c
Oracle JDBC Driver (ojdbc11.jar)
Configuración de Oracle XE
Connection String
jdbc:oracle:thin:@localhost:1521:XE
Usuario Oracle
system
Contraseña
12345

Autor
Proyecto educativo Java + Oracle 19c
Condicionales Anidados con JDBC :. . / .  
